# PROCESS.md — 항해일지 생성 프로세스 & 진행 단계 정의

> 목적: 대시보드와 목차 상세페이지에 **진행률**을 표시하기 위해, 항해일지 제작 과정을 기계가 판정 가능한 단계로 확정한다.
> PRD FR-021(작업 상태 관리)·FR-032(대시보드)의 구현 기준 문서다. PRD와 충돌하면 이 문서가 구현 기준이다.

---

## 1. 프로세스 전체 구조 — 두 개의 트랙

진행률을 하나의 숫자로 뭉치면 안 된다. 작업 **단위가 다르기 때문**이다.

| 트랙 | 단위 | 성격 | 주기 |
| --- | --- | --- | --- |
| **A. 수기 풀 준비** | **기수(cohort)** | 수기 수집 → AI 평가. 목차와 무관하게 1회 수행 | 기수당 1회 |
| **B. 원고 제작** | **목차(toc)** | 목차 입력 → 선별 → 탈고 → 확정 → export | 목차마다 반복 |

- A는 B의 **선행 조건**이다. A가 끝난 기수의 수기만 B의 후보 풀이 된다.
- 대시보드/상세페이지의 "진행 단계"는 **B(원고 제작) 6단계**를 뜻한다. A는 별도 "데이터 준비 현황" 블록으로 표시한다.
- 롤업 순서: `수기(manuscript) → 목차(toc) → 호차(issue) → 프로젝트(연도)`

---

## 2. 트랙 A — 수기 풀 준비 (기수 단위)

| ID | 단계 | 완료 조건 (데이터 판정) | 현재 상태 |
| --- | --- | --- | --- |
| A1 | 수기 수집·업로드 | 해당 기수 `authors` 행 존재 + `submissions` 행 존재 | ✅ 5·7·8·9기 적재 완료 (authors 527) |
| A2 | AI 평가 | `evaluations` 행 존재 (author 기준) | 🟡 502 / 527 (95.3%) |
| A3 | AI 선별 판정 | `evaluations.is_selected IS NOT NULL` | ✅ 0009 마이그레이션에서 일괄 backfill |

**기수 준비율** = `평가 완료 author 수 / 전체 author 수` (기수별로 계산)

---

## 3. 트랙 B — 원고 제작 6단계 ★ 진행 단계의 본체

### 3.1 단계 정의

목차 상세페이지에 표시되는 스테퍼는 아래 6단계다.

```
S1 목차 입력 → S2 AI 수기 선별 → S3 편집자 수기 확정 → S4 AI 원고 탈고 → S5 편집자 원고 확정 → S6 EXPORT
   [편집자]       [AI]              [편집자]              [AI]              [편집자]              [산출]
```

**AI ↔ 편집자가 번갈아 진행하는 구조**다. 홀수 단계는 사람이 입력·확정하고, 짝수 단계는 AI가 실행한다. 각 AI 단계의 결과물은 반드시 다음 편집자 단계에서 확정을 거친다(PRD FR-026: AI 결과는 최종값이 아니다).

| ID | stage key | 단계명 | 완료 조건 (데이터 판정) | 판정 단위 | 가중치 |
| --- | --- | --- | --- | --- | --- |
| S1 | `toc_input` | 목차 입력 | `ax_toc.toc_content` · `hanmadi` · `approved_question_ids` 모두 non-empty | 목차 | 10 |
| S2 | `ai_shortlist` | AI 수기 선별 | `ax_toc.shortlisted_at` non-null | 목차 | 10 |
| S3 | `editor_pick` | 편집자 수기 확정 | `ax_manuscript` 행 수 ≥ `select_count` | 원고 | 15 |
| S4 | `ai_revise` | AI 원고 탈고 | `edited_text` · `subtitle` · `comment` 모두 non-empty | 원고 | 25 |
| S5 | `editor_final` | 편집자 원고 확정 | `status = 'final'` | 원고 | 35 |
| S6 | `export` | EXPORT | `ax_toc.exported_at` non-null | 목차 | 5 |
| — | | **합계** | | | **100** |

각 단계가 흡수한 작업:

- **S1 목차 입력** — 목차명·Part/Chapter·목차내용·한마디 작성 + **question DB 질문 승인**(FR-006). 편집자가 AI를 돌리기 전에 한 화면에서 끝내는 준비 작업이라 한 단계로 묶는다.
- **S4 AI 원고 탈고** — 탈고(FR-012) + **소제목 생성**(FR-016) + **comment 생성**(FR-015) + 밑줄 추천(FR-017). 전부 AI 생성물이므로 한 단계다.
- **S5 편집자 원고 확정** — 원문/탈고문 비교 검토, comment·소제목 확인·수정, 밑줄 조정, 학생 정보 매칭 검수(FR-011) 후 확정.

### 3.2 설계 원칙 3가지

1. **단계는 `status` 문자열이 아니라 필드 존재로 자동 판정한다.** 편집자가 저장만 하고 상태 전환을 잊어도 진행률이 실제와 어긋나지 않는다. 예외는 S5 하나 — 이건 사람의 **명시적 확정 신호**여야 하므로 `status='final'`로만 판정한다.
2. **밑줄(`highlights_json`)은 S4 완료 조건에서 제외**한다. 밑줄이 없는 게 정상인 수기가 있어 필수화하면 진행률이 영구히 100%에 못 미친다. 밑줄은 표시용 부가 지표로만 센다.
3. **`검수 필요`는 단계가 아니라 플래그다.** PRD FR-021은 `검수 필요`를 다른 상태들과 같은 enum에 넣었지만, 진행 순서상의 위치가 아니라 "S5 확정을 막는 경고"다. 별도 컬럼 `ax_manuscript.needs_review`(0/1)로 분리하고, 스테퍼가 아니라 **경고 배지**로 표시한다.

### 3.3 진행률 계산식

```
목차 진행률(%) = Σ (가중치_i × 완료율_i)

  목차 단위 단계(S1·S2·S6):  완료율 = 0 또는 1
  원고 단위 단계(S3):         완료율 = min(1, 선별 원고 수 / select_count)
  원고 단위 단계(S4·S5):      완료율 = 조건 충족 원고 수 / max(1, 선별 원고 수)
```

- **S4·S5의 분모는 `select_count`가 아니라 실제 선별 원고 수**다. 편집자가 목표보다 적게 뽑고 마감하는 경우가 있어, 목표치로 나누면 진행률이 100%에 도달하지 못한다.
- **현재 단계** = 완료율이 1 미만인 가장 낮은 단계. 전 단계 완료면 `완료`.
- **호차 진행률** = 목차별 진행률의 `select_count` 가중 평균 (수기 10개짜리 목차와 2개짜리 목차를 동일 취급하지 않기 위함).
- **프로젝트 진행률** = 호차 진행률의 단순 평균.

### 3.4 PRD FR-021 상태값 ↔ 6단계 매핑

| PRD FR-021 상태값 | 본 문서 |
| --- | --- |
| 업로드 완료 | A1 |
| AI 평가 완료 | A2 |
| AI 선별 완료 | A3 + S2 |
| 편집자 선별 완료 | S3 |
| 탈고 완료 | S4 (소제목·comment 생성 포함) |
| 최종 확정 | S5 |
| export 완료 | S6 |
| 검수 필요 | **단계 아님 → `needs_review` 플래그** |

---

## 4. 화면 표시 설계

### 4.1 목차 상세페이지 (`/ax/toc/[tocId]`)

상단에 **6단계 스테퍼**를 고정 표시한다.

```
 ●━━━━━● ━━━━━ ○ ─────  ○ ─────  ○ ─────  ○
 목차입력  AI선별   편집자    AI탈고   편집자   EXPORT
  완료     완료    수기확정            원고확정
                   8/10
```

- 완료 단계는 채움, **현재 단계는 강조 + 분수 표시**(`8/10`), 이후 단계는 흐림.
- 스테퍼 아래에 목차 진행률 바 1개 + `needs_review` 건수 경고 배지.

### 4.2 대시보드 (`/dashboard`)

1. **헤더**: 프로젝트/호차 선택 + 전체 진행률 바 (호차 가중 평균)
2. **퍼널 카드 5개** — 6단계와 이름을 맞춘 수기 개수 퍼널
   `후보 → 수기확정 → 탈고 → 원고확정 → export`
   현재 값: 후보 502 · 수기확정 11 · 탈고 2 · 원고확정 1 · export 0
   *(탈고 2 = `edited_text`만 있는 건 3건이나, 소제목·comment까지 갖춘 S4 완료는 2건)*
3. **목차별 테이블**

   | 목차 | 현재 단계 | 진행률 | 수기확정 | 탈고 | 원고확정 | 검수필요 |
   | --- | --- | --- | --- | --- | --- | --- |
   | Part 2 Ch 1. 상반기 공부법 | AI 원고 탈고 | ▓▓▓▓▓░░░ 54% | 8/10 | 3/8 | 1/8 | ⚠ 1 |

4. **데이터 준비(트랙 A)** — 기수별 업로드/평가 완료율 (기존 "기수별 현황" 테이블에 `평가 완료율` 컬럼 추가로 흡수)

### 4.3 색상 규칙

- 진행률 바: `0% 회색 / 1–99% accent / 100% green`
- `needs_review > 0`인 목차는 진행률과 무관하게 **경고 배지**를 함께 표시하고, 100%여도 green으로 칠하지 않는다.

---

## 5. 필요한 스키마 변경 (migration `0010_ax_progress.sql`)

현 스키마로는 **S1의 질문 승인 부분·S2·S6과 검수 필요 플래그를 판정할 수 없다.** S3·S4·S5는 지금 데이터로 즉시 계산 가능하다.

```sql
-- 목차 단위 단계 판정용
ALTER TABLE ax_toc ADD COLUMN approved_question_ids TEXT;   -- S1: 승인 질문 id JSON 배열
ALTER TABLE ax_toc ADD COLUMN shortlisted_at TEXT;          -- S2: AI 수기 선별 실행 시각
ALTER TABLE ax_toc ADD COLUMN exported_at TEXT;             -- S6: 최종 docx export 시각

-- 검수 필요 플래그 (단계가 아닌 경고)
ALTER TABLE ax_manuscript ADD COLUMN needs_review INTEGER NOT NULL DEFAULT 0;
ALTER TABLE ax_manuscript ADD COLUMN review_note TEXT;
```

> D1 주의: `ALTER TABLE … ADD COLUMN`은 재실행 시 `duplicate column` 에러가 난다. 마이그레이션은 1회만 적용하고, 트랜잭션(`BEGIN`/`COMMIT`)은 쓰지 않는다.

**export 이력**은 지금은 `ax_toc.exported_at` 한 컬럼으로 충분하다. 재export 이력·파일명 규칙(FR-020)·다운로드 감사 로그가 필요해지면 그때 `ax_export` 테이블로 승격한다. (지금 만들면 빈 테이블만 남는다.)

---

## 6. 구현 순서

| 순서 | 작업 | 비고 |
| --- | --- | --- |
| 1 | `migrations/0010_ax_progress.sql` 적용 | 위 5개 컬럼 |
| 2 | `src/lib/ax-progress.ts` — 6단계 정의·가중치·진행률 계산을 한 파일에 집약 | 단일 소스. UI/API가 각자 계산하지 않게 함 |
| 3 | `src/lib/queries/ax-progress.ts` — 목차별 집계 쿼리 1개 | 목차당 1행, 원고 카운트는 서브쿼리 |
| 4 | `StageStepper` 컴포넌트 → 목차 상세페이지 상단 | 6단계 스테퍼 |
| 5 | 대시보드에 퍼널 + 목차별 진행 테이블 추가 | `src/app/dashboard/page.tsx` |
| 6 | 기존 화면에 단계 기록 쓰기 경로 연결 | 선별 실행→`shortlisted_at`, export→`exported_at`, 질문 승인 UI→`approved_question_ids` |

6번을 빼먹으면 S1·S2·S6이 영원히 미완료로 남는다. 스키마만 추가하고 쓰기 경로를 안 붙이는 게 가장 흔한 실패다.
