# 시대인재 Design System

> 시대인재 패밀리사이트를 위한 디자인 시스템.
> 브랜드 아이덴티티 가이드라인(2025-06)과 라이브 사이트에서 실측한 UI 규격을 한 문서에 정의합니다.
> **이 파일은 항해일지 AUTO(AX MVP) 적용본이다.** 원본은 같은 폴더의 `sdij-design.md`(v3.3, 시대인재 시간표 프로젝트에서 관리)이며, 여기서는 시간표 전용 컴포넌트를 빼고 §6.2에 AX 적용 규칙을 더했다. 브랜드·파운데이션 규칙이 바뀌면 원본을 먼저 고치고 이 파일에 반영한다.

**Version** 3.3-AX (2026-09-14) · **국문 서체** Pretendard Variable · **영문 서체** Neue Haas Unica W1G · **운영** 주식회사 하이컨시(HICONSY)

---

## Table of Contents

1. [Overview](#1-overview)
2. [Brand Concept](#2-brand-concept)
3. [Basic Identity System](#3-basic-identity-system)
4. [Graphic Motif](#4-graphic-motif)
5. [Foundations](#5-foundations)
6. [Surfaces](#6-surfaces)
7. [Components](#7-components)
8. [Patterns](#8-patterns)
9. [Content & Voice](#9-content--voice)
10. [Accessibility](#10-accessibility)
11. [Assets & Resources](#11-assets--resources)
12. [Changelog](#12-changelog)

---

## 1. Overview

### 1.1 목적

이 문서는 시대인재 패밀리사이트 전반에서 일관된 브랜드·UI를 유지하기 위한 단일 기준점입니다. 디자이너와 개발자가 동일한 토큰과 컴포넌트 규격을 공유하도록 합니다.

### 1.2 적용 범위

| 사이트 | 도메인 | 성격 | 표면(Surface) |
| --- | --- | --- | --- |
| 시대인재 학원 | `sdij.com/aca` | 학원 본 사이트 | Marketing |
| 시대인재N | `sdij.com/sdn` | 온라인·N 라인 | Marketing |
| 시대인재 재수종합 | `sdijc.com` | 재수종합반 | Marketing |
| 시대인재 BOOKS | `sdijbooks.com` | 교재·출판 | Marketing |
| 운영 관리자 | `*-admin.hiconsysvc.com` | 내부 관리 콘솔 | Application |

`sdij.com/aca`는 캠퍼스별 파라미터(`?campus=101` 대치 / `102` 분당 / `104` 목동 / `107` 대전 / `109` 반포)로 같은 화면을 공유합니다. 모바일은 `m.sdij.com`으로 분기하며, 데스크톱 사이트 자체도 반응형입니다.

### 1.3 출처 표기

| 태그 | 의미 |
| --- | --- |
| `[가이드]` | 브랜드 아이덴티티 가이드라인 원본 규정 — **임의 변경 금지** |
| `[관측]` | 라이브 사이트 CSS·화면에서 직접 측정한 값 |
| `[제안]` | 시스템 일관성을 위해 파생·정의한 값 — 운영하며 보완 가능 |
| `[적용·시간표]` | 시대인재 시간표 MVP에 적용하며 정한 규칙. AX에서 그대로 재사용 가능한 것만 남겼다 |
| `[적용·AX]` | 항해일지 AUTO(이 저장소)에 적용하며 정한 규칙 — §6.2 |

**정본 문서** — `SDIJ_20250605_SDIJ_Guideline_Ver_1.pdf` (Brand Identity Guidelines Version 1.0, June 2025, 55p, DESIGNED BY CLAY INC., ©2025 SDIJ). 벡터 자산은 `SDIJ_20250605_SDIJ_Vector_Assets_Ver_1.ai`.
가이드라인 자체가 **사외 반출 금지** 문서이며, 수록 이미지는 내부 열람용입니다. 수정·추가 제작이 필요하면 브랜드 담당 부서와 협의합니다.

**관측 범위(2026-09-06)** — `sdij.com/aca` 홈·시간표·입시설명회·컨설팅·공지사항(목록/상세)·부엉이포스트·비즈니스 제안, 데스크톱(1568px)·모바일(390px), 공통 CSS 13종(`/common/css/*`).

---

## 2. Brand Concept

### 2.1 Brand Story `[가이드]`

> 시대인재는 시대의 정신이 되는 인재를 육성하기 위해, 완벽한 성취도를 달성할 수 있도록 설계된 **고도화된 교육 엔진**입니다.

> The owl spreads its wings only with the falling of the dusk, whereas the owls of SDIJ soar high at the shining break of day for brave new world.
> 미네르바의 부엉이는 황혼이 저물어야 그 날개를 펴지만, **시대인재의 부엉이는 새벽놀에 새로운 세계를 향해 비상한다.**

이 문장이 브랜드의 시간 은유(새벽)를 규정하며, §5.1 컬러 팔레트("새벽의 빛 스펙트럼")의 근거입니다.

### 2.2 Brand Platform `[가이드]`

| 층위 | 내용 |
| --- | --- |
| VISION / PHILOSOPHY | 교육을 통한 인간 행복의 실현 |
| MISSION | 시대의 정신이 되는 인재(Human Resource)의 육성 |
| SLOGAN | **Navigate to Perfection** |
| BRAND ESSENCE | **자기초극** — 개인의 잠재력과 한계를 초극한 성공의 실현 |

**CORE VALUES (3)**

| 가치 | 국문 | 실체 |
| --- | --- | --- |
| Quality Contents | 최상의 교육 콘텐츠 | 시대인재 콘텐츠, 서바이벌, 파밍 시스템 |
| Effective Embodiment | 효율적 체화 방법론 | 이미지 트레이닝, 공간, 현장성, 실전력 |
| Adaptive Navigation | 개인화된 내비게이션 | VOD 서비스, AI 저작 솔루션 |

세 가치는 **SHORTCUT**으로 연결되어 BRAND ESSENCE(자기초극)를 감싸고, 그 토대가 **EDUCATION ENGINE**(REAL-TIME · CONCURRENCY)입니다. 다이어그램을 재현할 때 이 위상(3축 → SHORTCUT → 에센스 → 엔진)을 유지합니다.

### 2.3 Contents Experience `[가이드]`

| Phase | STEP | 단계 | 내용 | 해당 콘텐츠 예시 |
| --- | --- | --- | --- | --- |
| 1. 이해 | STEP 1 | 개념 | — | — |
| 1. 이해 | STEP 2 | 적용 | 배운 개념을 문제에 활용, 이해의 확장 | Truss / Essence |
| 2. 성취 | STEP 3 | 스킬 | 빠른 문제풀이 요령 | — |
| 2. 성취 | STEP 4 | 훈련 | 반복 학습으로 정확도와 속도 향상 | Bridge / Vortex |
| 3. 숙련 | STEP 5 | 실전 | 실력 최종 점검 및 완성 | Survival |

축은 두 개입니다 — **Lecture-Centered**(강의 중심, 강사의 명확한 설명이 핵심)와 **Content-Centered**(콘텐츠 중심, 스스로 풀고 익히는 개인의 의지가 핵심). STEP 1·3은 강의 중심, STEP 2·4·5는 콘텐츠 중심입니다.

---

## 3. Basic Identity System

### 3.1 Brand Mark 구성 `[가이드]`

가이드라인이 정의하는 로고 구성 요소는 **5종**입니다. 명칭을 임의로 바꾸지 않습니다.

| # | 구성 요소 | 설명 |
| --- | --- | --- |
| 2.1.1 | **Symbol** (심볼) | 피라미드 속 에너지가 응축되는 중심 **Vortex**의 상징 = 브랜드 에센스 '자기초극'. 동시에 학원명 초성 **ㅅ · ㄷ · ㅇ · ㅈ**을 형상화. 개별적이면서 하나로 연결된 형태 |
| 2.1.2 | **Korean Wordmark** (국문 워드마크) | `시대인재` |
| 2.1.3 | **Brand Signature** (브랜드 시그니쳐) | 심볼 + 워드마크 조합. **사용 1순위** |
| 2.1.4 | **Latin Wordmark – All Caps** | 영문 대문자 워드마크 |
| 2.1.5 | **Latin Wordmark – Lowercase** | 영문 소문자 워드마크(Ⓡ 유/무 2종) |

> ⚠️ **영문 워드마크는 심볼과 조합해 사용하지 않습니다.** (가이드 2.1.4 / 2.1.5 명시)
> 심볼의 의미는 "삼각형 기하 도형"이 아니라 **피라미드 · Vortex · 초성 4자**입니다.

### 3.2 Clear Space & Minimum Size `[가이드]`

**Clear Space는 전 요소 공통 `0.4x`** — 여기서 `x`는 해당 마크의 높이입니다. 사방(상하좌우) 최소 `0.4x`를 비웁니다.

| 요소 | 최소 크기 (300dpi) | 최소 크기 (72ppi) |
| --- | --- | --- |
| Symbol | 5mm | **14px** |
| Brand Signature | 5mm | **14px** |
| Korean Wordmark | 4mm | **11px** |
| Latin Wordmark – All Caps | 4mm | **11px** |
| Latin Wordmark – Lowercase (+Ⓡ) | 8mm | **22px** |
| Latin Wordmark – Lowercase (Ⓡ 생략) | 3mm | **8.5px** |

- 브랜드 시그니쳐가 **5mm/14px 이하**로 표현될 때는 로고타입을 제한적으로 사용할 수 있습니다.
- 국문·영문 워드마크가 **4mm/11px 이하**일 때는 각 워드마크를 제한적으로 사용할 수 있습니다.
- 소문자 워드마크가 **8mm/22px 이하**면 Ⓡ 없는 워드마크를 쓰고, **정렬 시 Ⓡ을 제외한 워드마크 영역을 기준**으로 정렬합니다.

### 3.3 Usage Priority `[가이드]`

| 순위 | 사용 |
| --- | --- |
| **1순위** | 브랜드 시그니쳐 |
| **2순위** | 국문 워드마크 · 영문 워드마크 |
| 제한 사용 | **심볼** — 사이니지·아이콘 등 브랜드를 상징적으로 표현해야 하는 경우에만 |
| 제한 사용 | **브랜드 시그니쳐 세로형** — 사이니지 제작 시에만 |
| 제한 사용 | **굿즈용 워드마크** — 굿즈 제작 시에만(작은 크기에서는 Ⓡ 생략) |

적용 환경에 따라 우선순위가 달라질 수 있으며, 그 경우 브랜드 담당 부서와 협의해 결정합니다.

### 3.4 Logo Color `[가이드]`

배경별 로고 색 조합은 **3순위 체계**입니다.

| 순위 | 규정 | 허용 조합 |
| --- | --- | --- |
| **1순위** | SDIJ Blue Black & White 팔레트 | 배경 White + 로고 Blue Black / 배경 Blue Black + 로고 White |
| **2순위** | SDIJ Gray 팔레트 | 배경 Blue Black + 로고 Light Gray / 배경 White + 로고 Cool Gray / 배경 White + 로고 Deep Gray |
| **3순위** | 시대인재 지정색으로 통제할 수 없는 환경 | 일반 흰색 배경 + 검은 로고 / 검은 배경 + 흰 로고 |

판단이 어려우면 브랜드 담당 부서와 협의합니다.

> ❌ **이전 버전(v2.0)의 `SDIJ Blue #0077C1`, `Ink #231F20`은 가이드라인에 존재하지 않는 값이었습니다. 삭제했습니다.**

### 3.5 Restrictions — Don't `[가이드]`

- 로고의 **간격과 비율**을 임의로 조정하지 않습니다.
- 로고의 **형태를 변경하거나 왜곡**하지 않습니다.
- **지정된 조합 이외의 조합**(예: 심볼+영문 워드마크)을 사용하지 않습니다.
- 로고를 **임의로 회전**하지 않습니다.
- 로고 **컬러를 임의로 조합**하지 않습니다.
- 로고의 **시인성을 저해하는 효과**(그림자·글로우 등)를 적용하지 않습니다.
- 로고에 **이미지 마스킹** 효과를 적용하지 않습니다.
- 로고의 **시인성이 저해되는 배경** 위에 올리지 않습니다.

### 3.6 Sub-Brands `[가이드]`

수능 중심 브랜드와 확장 브랜드는 **서로 다른 서체 시스템**을 써서 변별력을 만듭니다.

| 구분 | 영문 | 국문 |
| --- | --- | --- |
| 수능 중심 서브 브랜드 | Neue Haas Unica W1G **Bold** | Pretendard Variable **두께 543** |
| 확장 서브 브랜드 | Neue Haas Unica W1G **Regular** | Pretendard Variable **두께 543** |

락업 비율(가이드 실측): 수능형 `x` 기준 심볼-텍스트 간격 `.3x`, 텍스트 폭 `.82x`, 국문 병기 시 `.35x`/`.74x`. 확장형은 `.4x`/`.7x`, 국문 확장형 `.35x`/`.77x`.
가이드에 수록된 서브 브랜드명은 **예시**이며, 신규 서브 브랜드는 이 규정을 참고해 제작합니다.

### 3.7 Affiliate & Collaboration `[가이드]`

- 시대인재 로고와 협업사 마크를 병기할 때 **구분선 두께 `.03x`**, 좌우 여백 각 `.7x`.
- 로고·마크 간 크기와 형태 차이를 고려해 **시대인재 로고가 위축돼 보이지 않도록 시각보정**합니다.

### 3.8 Owl Motif (부엉이) `[관측]`

부엉이는 §2.1 브랜드 스토리에서 유래한 상징이며 콘텐츠 채널 **부엉이포스트**(`/aca/owlPost/`)로 노출됩니다. 보조 내러티브·일러스트 모티브로 쓰되 §3.1 정식 로고를 대체하지 않으며, 가이드라인의 그래픽 모티프는 §4의 **Grid Line / Penrose** 두 가지뿐입니다.

---

## 4. Graphic Motif `[가이드]`

그래픽 모티프는 사진·문구와 함께 브랜드 이미지를 표현합니다. 판단이 어려우면 브랜드 담당 부서와 협의합니다.

### 4.1 Grid Line (그리드 라인)

**제작 규정**

- 모티프 적용 가능 범위는 **총 너비·높이의 80% 이내** (상하좌우 각 10% 여백).
- 선 굵기는 **0.5pt와 1pt를 기본**으로 하며, 대지가 넓어지면 시각적으로 조정 가능.
- 원(포인트)의 크기는 **판형 짧은 면의 1%~3%**, 선 색상·두께는 가로/세로 그리드 라인과 동일하게.
- 그리드 선은 **수직·수평을 유지**합니다.

**활용 3형**

| 유형 | 규칙 |
| --- | --- |
| A. 색상 구분 사용 | 과도한 대비를 주지 않는 선에서 |
| B. 이미지 박스 그리드 | 이미지보다 선이 더 드러나거나 충돌하지 않는 선에서 |
| C. 확장 그리드 | **어두운 배경 위에서는 그리드 선이 배경보다 밝아야** 함 |

판형별(정방형·가로형·세로형) 모두 80% 규칙을 동일하게 적용합니다.

### 4.2 Penrose (펜로즈)

- 펜로즈 모티프들의 **굵기는 서로 동일**하게 적용합니다.
- 색상 구분 사용 시 **과도한 대비를 주지 않습니다.**
- **A. 이미지형** — 펜로즈 도형을 배경 이미지로 활용(Type A-1 / A-2).
- **B. 프레임형** — 판형에 맞추어 내용을 배열(Type B-1 / B-2).
- 패키지 등 특별한 인쇄가 필요한 경우 모티프를 **선으로만** 표현할 수 있습니다.

> 웹 적용 시: 두 모티프 모두 라이브 사이트에는 **아직 적용돼 있지 않습니다**(§5.7 갭 참고).

---

## 5. Foundations

### 5.1 Color

브랜드 색상은 **"하루 중 가장 먼저 변화가 시작되는 시간, 새벽의 빛"** 을 모티프로 합니다. 짙은 어둠부터 빛이 뜨기 전까지의 스펙트럼으로 시작–성장–완성을 표현합니다. `[가이드]`

#### 5.1.1 Primary Brand Color `[가이드]`

4계열 × 4단계. **가장 좌측(100%) 색상을 최우선 브랜드 컬러로 사용**하고, 필요 시 틴트로 보조합니다.

| 계열 | 100% | 80% | 60% | 30% | Pantone | 역할 `[제안]` |
| --- | --- | --- | --- | --- | --- | --- |
| **SDIJ Light Gray** | `#DBDFDE` | `#E3E5E5` | `#F0F1F1` | `#F5F5F5` (SDIJ White) | 427 C | 라이트 서피스·구분 |
| **SDIJ Cool Gray** | `#77878C` | `#919EA3` | `#ADB8BA` | `#D6DBDB` | 2164 C | 보조 텍스트·아이콘 |
| **SDIJ Deep Gray** | `#293B47` | `#54616B` | `#808A91` | `#BFC4C7` | 2377 C | 다크 서피스·강조 텍스트 |
| **SDIJ Blue Black** | `#010D19` | `#343D47` | `#676E75` | `#B2B6BA` | 296 C | **프라이머리.** 로고 1순위 색 |

CMYK(가이드 명시): Light Gray `C17 M10 Y12 K0` · Cool Gray `C61 M45 Y42 K0` · Deep Gray `C87 M74 Y61 K33` · Blue Black `C96 M90 Y75 K68`.

> ⚠️ **원본 PDF 오탈자 2건 — 아래 값이 실제 스와치입니다(픽셀 실측 확인).**
> ① `SDIJ Light Grey 80`: PDF 표기 `e3d5e5` → 실제 **`#E3E5E5`** (PDF 자체 RGB 표기 `227 229 229`와도 일치)
> ② `SDIJ Cool Gray 30`: PDF 표기 `77878c`(100% 값 중복 오기) → 실제 **`#D6DBDB`** (PDF RGB 표기 `214 219 219`와 일치)
> ③ `SDIJ Light Gray`의 RGB 표기 `R219 G233 B222`는 hex `dbdfde`(=219·223·222)와 불일치 — **hex가 정본**입니다.
> 인쇄 발주·시안 검수 시 이 3건을 먼저 확인하십시오.

#### 5.1.2 Sub-Brand Color `[가이드]`

서브 브랜드 컬러는 **메인 브랜드와 혼용하지 않고**, 지정된 서브 브랜드를 나타낼 때만 사용합니다.

| 계열 | 100% | 80% | 60% | 30% | Pantone |
| --- | --- | --- | --- | --- | --- |
| **Sky Blue** | `#94BDD4` | `#A8C9DB` | `#BFD6E5` | `#DEEBF2` | 291 C |
| **Navy** | `#052645` | `#385269` | `#697D8F` | `#B5BFC7` | 295 C |
| **Teal** | `#0A5457` | `#3B7578` | `#6B9999` | `#B5CCCC` | 2213 C |
| **Purple** | `#38268A` | `#6152A1` | `#877DB8` | `#C2BDDB` | 2104 C |

(80/60/30 틴트 값은 가이드에 수치가 없어 스와치를 픽셀 실측한 값입니다. `[관측]`)

#### 5.1.3 라이브 사이트 실사용 그레이 `[관측]`

**사이트 CSS에는 CSS 변수(`:root --token`)가 없고 값이 하드코딩돼 있습니다.** 실제로 쓰이는 색은 브랜드 팔레트가 **아니라** 순수 무채색입니다.

| HEX | 빈도 | 용도 |
| --- | --- | --- |
| `#000000` | 64 | 본문·제목, Primary 버튼, 활성 칩, 푸터 배경 |
| `#FFFFFF` | 33 | 페이지 배경, 활성 탭, 반전 텍스트 |
| `#D4D4D4` | 31 | 보더, placeholder, disabled 배경 |
| `#767676` | 15 | 보조 텍스트(카운터 current) |
| `#9F9F9F` | 13 | 비활성 칩·페이지네이션·disabled 텍스트 |
| `#F5F5F5` | 10 | 인포박스·비활성 탭·상세 헤더 배경 |
| `#EFEFEF` | 10 | 헤더/서브GNB 하단 보더, 구분선 |
| `#606060` | 8 | 캡션 |
| `#434343` | 6 | 푸터 내부 구분선 |
| `#052645` | 1 | 유일하게 등장하는 브랜드 계열 색(Navy) |

RGBA는 `rgba(0,0,0,.05)`(hover), `.1`(shadow), `.2/.3/.4/.5`, `.6`(모달 dim)만 사용합니다.

> **갭:** `#F5F5F5`는 우연히 `SDIJ White`와 같지만, `#000000`·`#767676`·`#9F9F9F`·`#D4D4D4`는 브랜드 팔레트에 없는 값입니다. 브랜드 정합을 맞추려면 `#000000 → #010D19`(Blue Black), `#767676 → #77878C`(Cool Gray), `#D4D4D4 → #D6DBDB`(Cool Gray 30) 치환을 검토하십시오. `[제안]`

#### 5.1.4 Application(관리자 콘솔) `[관측·미검증]`

v2.0에서 관리자 콘솔 캡처로 정리한 값입니다. **이번 조사 범위 밖이라 재확인하지 못했습니다.**

| Token | HEX | 용도 |
| --- | --- | --- |
| `console/chrome` | `#32373D` | 관리자 헤더·내비 바 |
| `console/section` | `#F2F3F5` | 카드/테이블 헤더 배경 |
| `accent/coral` | `#FC5163` | 주요·파괴적 액션 |
| `accent/indigo` | `#544E8C` | 보조 액션 |
| `status/success` | `#1F9D3D` | 성공 |
| `status/warning` | `#F39A0C` | 경고 |
| `status/error` | `#E5343B` | 오류 |

이 색들은 브랜드 가이드라인에 근거가 없습니다. **항해일지 AUTO의 현재 코드(`src/app/globals.css`)가 바로 이 v2.0 값을 쓰고 있다.** 재매핑 계획은 §6.2.

### 5.2 Typography

#### 5.2.1 지정 서체 `[가이드]`

| 용도 | 서체 | 웨이트 |
| --- | --- | --- |
| 국문 | **Pretendard Variable** | Bold / **Medium(기준)** / Light |
| 영문·숫자 | **Neue Haas Unica W1G** | Medium / **Regular(기준)** / Light |
| 전용 서체 | — | **개발 예정** (가이드 2.3.1) |

- **서체 적용이 제한되는 환경에서는 국·영문 모두 Pretendard를 사용합니다.**
- OpenType 지원 환경에서는 `단락 스타일 > 숫자 스타일 > **가변 폭 라이닝**`을 적용해 숫자 간격을 균일하게 맞춥니다.

#### 5.2.2 합성 글꼴 규정 `[가이드]`

합성 글꼴과 GREP을 허용하는 매체(메인 브랜드 커뮤니케이션 채널)의 타이포그래피는 다음과 같이 합성합니다.

| 요소 | 서체 |
| --- | --- |
| 국문 | Pretendard Variable **94% 축소** |
| 영문·숫자·문장부호 | Neue Haas Unica W1G |

정렬 기준은 **시각 정렬 중심선**입니다. 합성 글꼴 적용이 제한되면 **Pretendard 단독**으로 사용합니다.
적용 예시: `수능직전 10주 FINAL 모의평가`, `Master Plan 시즌제 Essential 패키지`, `[2026][시즌3] 언어이해 Total Care Final`.

#### 5.2.3 웹 타입 램프 `[관측]`

라이브 사이트에는 **19종의 명명된 텍스트 스타일**이 `/common/css/typography.css`에 정의돼 있습니다. 네이밍 규칙은 `{역할}-{크기}-{굵기}[-{행간×100}]`이며 굵기 약어는 `el`=200 · `l`=300 · `r`=400 · `sb`=600 · (13px만) `700`입니다.

| 클래스 | size | weight | line-height | letter-spacing |
| --- | --- | --- | --- | --- |
| `title-44-el` | 44px | 200 | 1.2 | -0.5px |
| `title-32-r` | 32px | 400 | 1.2 | -0.5px |
| `title-32-el` | 32px | 200 | 1.2 | -0.5px |
| `title-28-l` | 28px | 300 | 1.2 | -0.5px |
| `title-24-r` | 24px | 400 | 1.4 | -0.5px |
| `title-20-r` | 20px | 400 | 1.4 | -0.5px |
| `title-18-sb` | 18px | 600 | 1.4 | -0.5px |
| `title-18-r` | 18px | 400 | 1.4 | -0.5px |
| `body-18-r` | 18px | 400 | 1.4 | -0.5px |
| `body-18-r-180` | 18px | 400 | 1.8 | -0.5px |
| `body-16-sb` | 16px | 600 | 1.6 | -0.5px |
| `body-16-sb-180` | 16px | 600 | 1.8 | -0.5px |
| `body-16-r` | 16px | 400 | 1.6 | -0.5px |
| `body-16-r-180` | 16px | 400 | 1.8 | -0.5px |
| `body-14-sb` | 14px | 600 | 1.6 | 0 |
| `body-14-r` | 14px | 400 | 1.6 | 0 |
| `body-14-r-180` | 14px | 400 | 1.8 | 0 |
| `body-13-sb` | 13px | **700** | 1.6 | 0 |
| `body-13-r` | 13px | 400 | 1.6 | 0 |

규칙 3가지:
1. **16px 이상은 자간 `-0.5px`, 14px 이하는 `0`.** (한글 시각 보정)
2. **제목(title)은 행간 1.2~1.4, 본문(body)은 1.6, 긴 글은 `-180`(1.8) 변형.**
3. **위계는 굵기가 아니라 크기와 여백으로 만든다** — 가장 큰 44px이 가장 얇은 200(ExtraLight)입니다. 홈 히어로가 이 원칙의 전형입니다.

폰트 로드: `@import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css")`

```css
font-family: 'Pretendard', 'Noto Sans KR', 'Sans-serif', '맑은 고딕',
  '돋움', 'Dotum', 'Helvetica', 'Apple SD Gothic Neo';
```

> **갭:** 웹에는 **Neue Haas Unica W1G가 로드되지 않습니다.** 홈 히어로의 영문("The New Paradigm Navigate To Perfection")도 Pretendard로 렌더링됩니다. §5.2.1의 "제한 환경에서는 Pretendard 단독" 조항에 해당하지만, 브랜드 커뮤니케이션 핵심 화면이므로 웹폰트 라이선스 확보 여부를 확인할 필요가 있습니다.

### 5.3 Spacing `[관측]` + `[제안]`

CSS 실측 간격은 `4px` 배수로 정리됩니다: `2 · 4 · 8 · 10 · 12 · 16 · 20 · 24 · 30 · 40 · 60 · 80 · 120 · 240`.

| Token | px | 실사용 예 |
| --- | --- | --- |
| `space-1` | 4 | 탭 내부 gap |
| `space-2` | 8 | 칩 간격, 체크박스 gap |
| `space-3` | 12 | 칩 좌우 패딩 |
| `space-4` | 16 | 모바일 페이지 좌우 여백, 입력 패딩 |
| `space-5` | 20 | 버튼 좌우 패딩, 탭 패딩 |
| `space-6` | 24 | 인포박스 상하 패딩, 페이지네이션 gap |
| `space-8` | 32 | — |
| `space-10` | 40 | 푸터 상단 여백(모바일/태블릿) |
| `space-15` | 60 | 가이드 제목 하단 여백 |
| `space-20` | 80 | 섹션 간격, 푸터 패딩 |
| `space-30` | 120 | 모달 콘텐츠 하단 여백 |
| `space-60` | 240 | 상세 페이지 본문 하단 여백 |

### 5.4 Layout & Grid `[관측]`

| 항목 | 값 |
| --- | --- |
| 컨테이너 최대폭 | **1512px** (헤더·서브GNB·푸터 공통) |
| 데스크톱 좌우 패딩 | **100px** |
| 태블릿 푸터 폭 | `min(960px, 100% - 80px)`, `min-width: 688px` |
| 모바일 좌우 패딩 | **16px** |
| 상세 본문 폭 | **1080px** (`.container-view-body`) |
| 상세 헤더 | 배경 `#F5F5F5`, 패딩 `119px 0 80px` |
| 헤더 | `position: fixed`, 높이 **104px**, 배경 `#FFFFFF`, `z-index: 1000` |
| 서브 GNB | `position: fixed`, 높이 **50px**, 하단 보더 `1px #EFEFEF` |
| 푸터 | 배경 `#000000`, 패딩 `80px 100px`, 로고 높이 17px |

**브레이크포인트 — 3단계뿐입니다.** `[관측]`

| 구간 | 미디어 쿼리 | 사용 횟수 |
| --- | --- | --- |
| Desktop | `@media (min-width: 1280px)` | 95 |
| Tablet | `@media (min-width: 768px) and (max-width: 1279px)` | 50 |
| Mobile | `@media (max-width: 767px)` | 61 |

> ❌ v2.0의 `sm 640 / md 768 / lg 1024 / xl 1280 / 2xl 1536` 5단계와 `1200px` 컨테이너는 실제와 다릅니다. 삭제했습니다.

### 5.5 Border Radius `[관측]`

| 값 | 적용처 |
| --- | --- |
| `0` | 모바일 모달(풀스크린), 탭 |
| **`2px`** | 기본 버튼(`.btn`, `.btn-responsive`) |
| **`3px`** | 라지 버튼(`.btn-large`), 비밀번호 입력 |
| **`5px`** | 텍스트영역 박스(`.input-word-limit`) |
| `8px` | 가이드 페이지 카드 |
| `50%` | 원형 요소 |
| **`100px`** | 세컨더리 탭 칩(pill) |

> 요약: **버튼은 거의 각지고(2~3px), 필(pill)은 세컨더리 칩에만** 씁니다. v2.0의 "CTA는 완전 라운드"는 오류였습니다.

### 5.6 Elevation `[관측]`

사이트 전체에 그림자는 **2개뿐**입니다.

| 값 | 용도 |
| --- | --- |
| `0 4px 20px rgba(0,0,0,0.1)` | 드롭다운·떠 있는 패널 |
| `5px 9px 15px 0 rgba(0,0,0,.3)` | 팝업 |

구획은 그림자가 아니라 **보더(`#D4D4D4`·`#EFEFEF`)와 여백**으로 나눕니다. 로고에는 어떤 그림자도 금지입니다(§3.5).

### 5.7 Iconography `[관측]` + `[제안]`

- 아이콘은 **PNG/SVG 이미지 파일**로 삽입되며(`.pagination-arrow img { width:24px; height:24px }`), 아이콘 폰트를 쓰지 않습니다.
- 기본 사이즈 **24px**, 모달 닫기 버튼 **32px**, 체크박스·라디오 **20px**.
- 선형(line) 스타일, 색상은 텍스트 색을 따름. `[제안]`

---

## 6. Surfaces

### 6.1 Marketing Surface — 공개 사이트 `[관측]`

모노크롬 에디토리얼. 흰 배경 + 검은 텍스트, 강조도 검정. 유채색은 사실상 0.

| 의미 토큰 | 현재 값 | 브랜드 정합 값 `[제안]` |
| --- | --- | --- |
| `bg/base` | `#FFFFFF` | `#FFFFFF` |
| `bg/sunken` | `#F5F5F5` | `#F5F5F5` (SDIJ White) |
| `text/primary` | `#000000` | `#010D19` (Blue Black) |
| `text/secondary` | `#767676` | `#77878C` (Cool Gray) |
| `text/disabled` | `#9F9F9F` | `#919EA3` (Cool Gray 80) |
| `border/default` | `#D4D4D4` | `#D6DBDB` (Cool Gray 30) |
| `border/subtle` | `#EFEFEF` | `#F0F1F1` (Light Gray 60) |
| `action/primary` | `#000000` | `#010D19` |
| `action/primary-text` | `#FFFFFF` | `#FFFFFF` |
| `surface/inverse` | `#000000` (푸터) | `#010D19` |

### 6.2 Application Surface — 항해일지 AUTO (AX MVP) `[적용·AX]`

내부 편집국 도구다. 정보 밀도가 높은 운영 화면이며, Marketing 표면(§6.1)과 달리 **사이드바 + 카드 그리드** 골격을 쓴다. 브랜드 규칙(§3~§5)은 그대로 따르되, 표면 토큰은 아래 순서로 정리한다.

#### 6.2.1 현재 코드 토큰 `[관측]` (2026-09-14, `src/app/globals.css`)

v2.0 문서의 "관리자 콘솔 관측값"을 그대로 옮긴 상태다. **브랜드 가이드라인에 근거가 없는 값**에는 ⚠️를 붙였다.

| CSS 변수 | 값 | 용도 | 근거 |
| --- | --- | --- | --- |
| `--bg` | `#FFFFFF` | 페이지·카드 | ✅ |
| `--bg-subtle` | `#F5F5F5` | 섹션 배경 | ✅ = SDIJ White |
| `--bg-hover` | `#EDEDED` | hover | ⚠️ 팔레트 밖 |
| `--border` / `--border-strong` | `#E0E0E0` / `#CACACA` | 보더 | ⚠️ 팔레트 밖 |
| `--text` / `--text-muted` / `--text-faint` | `#141414` / `#5E5E5E` / `#808080` | 텍스트 3단 | ⚠️ 팔레트 밖 |
| `--section-header` | `#F2F3F5` | 카드·표 헤더 | ⚠️ v2.0 콘솔 관측 |
| `--sidebar-bg` = `--chrome` | `#32373D` | 사이드바 | ⚠️ v2.0 콘솔 관측 |
| `--accent` = `--coral` (+hover/pressed) | `#FC5163` | 주요 액션, 활성 내비 | ⚠️ v2.0 콘솔 관측 |
| `--indigo` | `#544E8C` | 3차 액션 | ⚠️ v2.0 콘솔 관측 |
| `--link` = `--brand-blue` | `#0077C1` | 링크·포커스 | ❌ **가이드에 없는 색**(§3.4) |
| `--green/--amber/--red` (+`-bg`) | `#1F9D3D` / `#F39A0C` / `#E5343B` | 상태 | ⚠️ 브랜드 밖, 관리자 상태색 관례 |
| `--radius-xs/sm/md/lg` | 4 / 6 / 8 / 12px | 모서리 | ⚠️ 사이트 실측은 2~5px(§5.5) |
| `--shadow-sm/md` | `0 1px 2px .06` / `0 4px 12px .08` | 그림자 | ⚠️ 사이트 그림자는 2종(§5.6) |
| `--font` | Pretendard → 시스템 한글 폴백 | 서체 | ✅ (§5.2.1 "제한 환경은 Pretendard 단독") |
| `body` | 14px / 1.5 | 기본 글자 | ✅ ≈ `body-14-r`(행간 1.6 권장) |

#### 6.2.2 브랜드 정합 재매핑 `[제안]`

UI 개편 때 아래로 교체한다. 원칙: **유채색은 상태 표시와 링크 한 곳뿐**, 나머지는 SDIJ 그레이 4계열(§5.1.1)로 만든다.

| 의미 토큰 | 현재 | 정합 값 | 비고 |
| --- | --- | --- | --- |
| `bg/base` | `#FFFFFF` | `#FFFFFF` | 카드 표면 |
| `bg/sunken` | `#F5F5F5` | `#F5F5F5` SDIJ White | 페이지 바탕(카드 뒤) |
| `bg/hover` | `#EDEDED` | `#F0F1F1` Light Gray 60 | |
| `section/header` | `#F2F3F5` | `#F0F1F1` Light Gray 60 | 표 헤더 |
| `border/default` | `#E0E0E0` | `#DBDFDE` Light Gray 100 | 카드·표 선 |
| `border/strong` | `#CACACA` | `#D6DBDB` Cool Gray 30 | 입력 보더 |
| `text/primary` | `#141414` | `#010D19` Blue Black | 19.6:1 |
| `text/secondary` | `#5E5E5E` | `#54616B` Deep Gray 80 | 7.4:1 |
| `text/tertiary` | `#808080` | `#77878C` Cool Gray | 3.73:1 — **14px 본문 금지**, 라벨·메타·아이콘만 |
| `surface/inverse` (사이드바) | `#32373D` | `#010D19` Blue Black 또는 `#293B47` Deep Gray | 로고 1순위 조합(White on Blue Black) |
| `action/primary` | ~~`#FC5163` 코랄~~ | **`#0074D2` 포인트** (2026-09-14 적용) | 제품 마크(favicon) 실측색. 코랄·인디고 삭제됨 |
| `action/destructive` | 코랄 | `#E5343B` (status/error) | 삭제·되돌릴 수 없는 동작만 |
| `link` / `focus` | ~~`#0077C1`~~ | **`#0074D2` 포인트** (2026-09-14 적용) | 포인트 하나로 통일. 12px 이하 글자·hover는 `#005BA8` |
| `wash-1` / `wash-2` | — | **`#EAF3FC` / `#D5E7F8`** (2026-09-14 신설) | 포인트의 옅은 단계. wash-1 = 행 hover·선택 행·표 헤더·배지 바탕, wash-2 = 선택 테두리·활성 칩·아바타 바탕 |
| `status/*` | 유지 | 유지 | 색만으로 구분 금지 — 아이콘·텍스트 동반(§10) |
| radius | ~~4/6/8/12~~ → **0 / 2 / 3 / 5** (2026-09-14 적용) | 확정 | xs 0(카드·표·배지·알림·진행바), sm 2(버튼), md 3(입력), lg 5(textarea·모달). 원형은 마크·단계 번호만 |
| shadow | 2종 | `0 4px 20px rgba(0,0,0,.1)` 1종 | 드롭다운·떠 있는 패널만. 카드는 보더로 구획 |

#### 6.2.3 확정 규칙 `[적용·AX]`

지금 코드에서 이미 지키고 있고 개편 후에도 유지하는 것.

- **서체** — Pretendard 로컬 설치본 우선, 없으면 시스템 한글 폰트. 원격 `@import`는 빌드·렌더를 막을 수 있어 쓰지 않는다(폰트 로드는 별도 `<link>`).
- **기본 글자** — 14px. 표·목록이 많은 관리자 밀도. 행간은 §5.2.3 램프대로 1.6.
- **서비스명** — 사용자 노출 이름은 **"항해일지 AUTO"**(2026-09-14 변경). "AX"는 코드·경로에만 쓴다.
- **제품 마크** — `favicon.ico.png` 원본(파란 원 + 흰 잎 마크)을 `src/app/icon.png`(파비콘)·`public/logo-mark.png`(사이드바 24px, 로그인 64px)로 쓴다. 시대인재 브랜드 로고(§3)와 별개의 제품 마크이며, 마크의 파란색은 마크 밖 UI에 쓰지 않는다.
- **문구** — 사용자 노출 텍스트에 중간 점(·)을 쓰지 않는다. 나열은 쉼표. 톤은 §9.2.
- **아이콘** — `lucide-react` 선형 아이콘만, 반드시 `src/components/ui/Icon.tsx` 래퍼로 쓴다. 크기 3단계 `sm` 12(배지·칩·표 안) / `md` 16(내비·버튼·알림, 기본) / `lg` 20(페이지 헤더·빈 상태), **선 굵기는 크기와 무관하게 1.5px 고정**(`absoluteStrokeWidth`), `currentColor`, 기본 `aria-hidden`. 개별 `size`·`strokeWidth` 지정 금지. 예외는 로그인 버튼의 Google 'G' 마크(공식 컬러 SVG) 하나.
- **카드** — 박스 보더·둥근 모서리·그림자 없이 **위 1px `border/default` 구분선 + 목록 아래 선**으로만 구획한다(원본 §7.14 규칙). 상하 패딩 20px, 좌우 0, 열 간격 24px, 호버는 윗선만 `text/primary`. 원고 대시보드에 적용(2026-09-14), 나머지 화면은 개편 회차에서 일괄.
- **로그인 페이지** — `bg/sunken` 바탕 위 흰 박스(위아래 구분선), 마크 64px, 제목 24/400, 버튼은 §7.4 `.btn` 규격(42px, radius 2px, outlined).
- **AI 결과 표시** — AI가 만든 값(선별 후보·교정·소제목·comment)은 편집자가 확정하기 전까지 **확정값과 시각적으로 구분**한다(PRD FR-026). 색이 아니라 라벨·배지로.
- **포인트 컬러 하나** — `--point #0074D2`(제품 마크 실측). 활성 메뉴, 주요 버튼, 링크, 진행바, 포커스 링, 단계 표시가 전부 이 색이다. 작은 글자·hover는 `--point-dark #005BA8`. 유채색은 이것과 `status/*`뿐이다.
- **wash** — `--wash-1 #EAF3FC`(행 hover, 선택 행, 표 헤더, 배지 바탕, 검색창 포커스 전 바탕은 회색 `bg/sunken`), `--wash-2 #D5E7F8`(선택 테두리, 활성 칩, 아바타 바탕). 넓은 면에는 wash-1만 쓰고 wash-2는 작은 면에만.
- **표 단일 규격** — 모든 목록은 `src/components/ui/DataTable.tsx`로 그린다. 글자 13px, 헤더 12px/600, 바탕 wash-2(행 hover의 wash-1보다 진하게), 글자 포인트 진한색, **모든 헤더 좌측 정렬**(숫자 열도 헤더는 왼쪽, 셀만 오른쪽), 고정, **행 높이는 40px로 통일**(헤더 36px, 셀 패딩 12px). 예외가 꼭 필요할 때만 `density`(comfortable 44px)를 쓴다. 헤더 라벨은 왼쪽(숫자 열은 오른쪽), **정렬 화살표와 필터 아이콘은 열 오른쪽 끝에 모아 둔다.** 행간 20px, 숫자 열은 우측 정렬 + 고정폭 숫자, 행 hover wash-1, 셀 안 두 줄 금지(긴 값은 `clip` 말줄임 + title, 표당 긴 글 열 하나는 `flex`로 남는 폭을 다 쓰고 줄어든다). 헤더 클릭 정렬(오름 → 내림), 정렬 아이콘은 라벨 뒤 12px. **열 필터**는 헤더 호버 시 나타나는 깔때기 → 값 목록 체크 패널(240px, 흰 바탕 + 그림자, 항목 hover wash-1), 활성이면 헤더 글자·깔때기가 포인트색이고 표 위에 필터 칩 줄이 붙는다. **표의 바깥 세로 테두리는 두지 않는다**(위아래 선만). 셀 유형(링크·배지·점수·진행률·금액)으로 렌더를 통일한다.
- **검색창** — `SearchInput`: 36px, 연한 바탕 `bg/sunken` + 보더 없음, 왼쪽 돋보기 16px, placeholder는 반드시 채운다("답변 본문이나 작성자 이름으로 검색"처럼 무엇을 찾는지). 포커스 시 흰 바탕 + 포인트 보더. Enter로 확정, 값이 있으면 지우기 X.
- **드롭다운** — `Dropdown`: 브라우저 기본 `<select>` 금지. 트리거 36px, 보더 1px, radius 3, 화살표는 오른쪽 끝 고정(열리면 180° 회전). 패널은 흰 바탕 + `0 4px 20px rgba(0,0,0,.1)`, 항목 hover wash-1, 선택 항목 포인트 진한색 + 체크. Esc·바깥 클릭으로 닫힘.
- **버튼** — 기본 32px(툴바·표 밖), radius 2px, 13px/600. 주요 동작은 포인트 필, 나머지 outlined. **아이콘은 하나만**: 실행·추가·저장·다운로드처럼 그 자리에서 일어나는 동작은 **앞에** 아이콘 하나(Plus, Pencil, Check, Download, Sparkles 등), 다음 화면으로 **이동**하는 버튼은 **뒤에** ArrowRight 하나. 앞뒤 둘 다 붙이지 않는다. 아이콘 크기는 16px(md) 고정, 텍스트와 간격 6px. **표 안의 동작은 박스 버튼이 아니라 밑줄 링크**(`.tbl-link`, 13px/500, 포인트 진한색, 밑줄 오프셋 3px, 아이콘 없음, 비활성은 회색 + 밑줄 없음).
- **사이드바** — 232px, 접으면 56px(아이콘만, 툴팁으로 라벨). 토글은 브랜드 줄 오른쪽, 상태는 브라우저에 기억. 하단 사용자 블록은 28px 원형 아바타(Google 사진, 없으면 이름 첫 글자를 wash-2 바탕에) + 이름·역할 + 로그아웃.
- **진행률 바** — 채움은 모두 검정(`text/primary`), 트랙은 `bg/hover`, 높이 8px(카드) / 6px(대시보드 막대). 상태별 색 구분(초록·주황) 없음. 검수 경고는 색이 아니라 배지로.
- **원고 대시보드** — 2컬럼(왼쪽 유연 + 오른쪽 320px)뿐. 왼쪽: KPI 4칸(구분선 카드) → 호차 카드(진행률 + 3단계 막대 + 목차 행) → 최근 편집 원고 표. 오른쪽: 할 일(최상단, 개수 배지 wash-1 + 링크) → AI 러너 → 데이터 준비 현황(기수 막대) → 발행 요약. **구분선은 섹션 시작(제목 위)에만** 두고 각 컬럼의 첫 섹션은 선이 없다. KPI 카드와 첫 호차 카드는 윗선 없이, 호차 카드 사이만 선. 할 일 개수 배지는 24px 정방형.
- **뒤로가기** — 상세 화면(호차·목차·원고·게재 원고·작성자)은 히어로 왼쪽 위에 `BackLink` 하나: `‹ 상위 화면의 제목` 그대로 쓴다(임의 문구·"목록" 버튼 금지). 목록 화면(대시보드·DB·환경설정)에는 없다.
- **히어로 제목** — 현재 객체의 전체 이름을 한 줄 h1(28px/700)로: "2027 항해일지 1호차", "Part 2 Chapter 1. 1년 공부몰입도", 학생 이름, 게재 원고 소제목. 상위 맥락은 뒤로가기에만 있고 eyebrow로 반복하지 않는다. 제목 아래 메타 줄은 13px, 항목은 16px 간격으로 구분(중간 점 금지). 공통 컴포넌트 `src/components/ui/Hero.tsx`.
- **표 열 필터 아이콘** — `ListFilter`(아래로 좁아지는 세 줄) 12px.
- **환경설정** — 항목형 목록(왼쪽 260px 이름·설명, 오른쪽 컨트롤, 항목 사이 1px 윗선, 그룹 제목 15px/700). 포인트 컬러는 한 항목: 36px 색 박스 + 프리셋 드롭다운(SDIJ 브랜드 팔레트 §5.1) + 직접 지정(hex·피커, 피커는 여백 없이 색이 박스를 채움) + 기본값. 진한색은 검정 22% 혼합, wash-1/2는 흰색 90%/80% 혼합, 포커스 링은 18% 투명. 브라우저별 저장(localStorage + 쿠키).
- **맨 위로 버튼** — 600px 이상 스크롤하면 오른쪽 아래 40px 사각 버튼(보더 + 그림자 1종), 화살표 아이콘.
- **페이지 끝 선 금지** — 카드 그리드 아래 구분선은 두지 않는다. 카드는 윗선만.
- **오류·404** — 셸 안에서 난 오류와 없는 경로는 사이드바를 유지한 채 본문 영역에만 표시한다(`(app)/error.tsx`, `(app)/not-found.tsx` + catch-all).
- **단계 줄(3단계)** — PROCESS.md 6단계를 같은 화면을 쓰는 것끼리 묶어 3단계로 보여준다: 1 목차 입력 / 2 수기 선별과 확정 / 3 원고 탈고와 확정. 세로 구분선·박스 없이 균등 배치(높이 54px), 22px 번호 원 배지(완료 = 포인트 채움 + 체크, 현재 = 포인트 테두리 + wash-1), 라벨 13px/600, 부제 11px에는 묶인 세부 단계 중 현재 단계와 분수. **열린 화면 하나 아래에만 2px 포인트 밑줄.** 모든 단계가 링크라 이전 단계로 언제든 돌아간다.
- **단계 액션 바** — 단계 줄 바로 아래(`StageBar`, 구분선 없음). 왼쪽은 요약 글자와 보조 버튼(outlined), **오른쪽 끝은 다음 단계로 가는 포인트 필 버튼 하나**. 세 화면(목차 입력·수기 선별·원고 탈고) 모두 같은 자리.
- **호차 히어로** — 제목은 "2027 항해일지 1호차"처럼 프로젝트와 호차를 **한 줄 h1**로(eyebrow 없음). 표지는 오른쪽 34%(최대 420px)에 `<img>`로 원래 비율·축소만(확대 금지), `object-fit: cover`로 위아래를 히어로 높이에 맞추고 **왼쪽 가장자리는 대각선(70°) 마스크 그라데이션으로 녹여 이미지 테두리가 드러나지 않게** 한다. 투명도 0.85. 파일 규약 `public/covers/<연도>-<호차번호>.jpg`, 매핑은 `src/lib/covers.ts`.
- **목차 카드(입력 폼)** — 박스 없이 윗선 1px만, 상하 패딩 18/8px. 카드 오른쪽 위는 배지 + 케밥(⋯) 메뉴(위로·아래로·삭제, 삭제는 빨강). 아이콘 단독 삭제 버튼은 쓰지 않는다.
- **케밥 메뉴** — 32px 버튼, 패널은 오른쪽 정렬, 흰 바탕 + `0 4px 20px rgba(0,0,0,.1)`, 항목 hover wash-1, 한 번에 하나만 열림, Esc·바깥 클릭 닫힘.
- **사이드바 토글** — 홑화살표(ChevronLeft/Right) 하나. 패널 모양 아이콘은 쓰지 않는다.
- **게재 원고 읽기** — 본문 15px/1.9, `keep-all`, 최대폭 720px + 오른쪽 300px 보조 열(목차 한마디·comment는 포인트색 왼쪽 선 인용 블록).
- **레이아웃 골격** — 좌측 고정 사이드바(232px, 접힘 56px) + 우측 콘텐츠. 6단계 진행(S1~S6, PROCESS.md §3.1)은 §7.5 `.steps` 규격을 따르는 단계 탭으로 콘텐츠 위에 둔다.

#### 6.2.4 UI 개편 착수 기록 (2026-09-14)

- **레퍼런스** — 대시보드형 관리자 레이아웃(Pinterest 핀, "SellPilot" 대시보드). 골격: 좌측 고정 사이드바(로고, 주 메뉴, 접히는 하위 메뉴, 구분선 아래 보조 섹션 + 뱃지 카운트, 하단 설정·프로필) / 상단 헤더(페이지 제목, 중앙 검색, 우측 알림·프로필) / 본문은 연회색 바탕 위 흰 카드 2컬럼 그리드(좌 넓게 차트·표, 우 좁게 KPI 카드·알림) / 얇은 보더, 큰 라운드, 포인트 컬러 1개, 아이콘 동반 상태 칩 / 표는 체크박스·정렬 화살표·상단 검색·뷰 전환.
- **브랜드 번역 원칙** — 레퍼런스의 그린 포인트는 그대로 쓰지 않는다. 포인트 컬러 자리는 `#010D19` Blue Black(필)과 `#052645` Navy(링크·선택) 두 개로 제한하고, 상태 칩만 `status/*`를 쓴다.
- **미확정** — 사이드바 색(현재 `#32373D` 유지). radius·카드 그림자·표 밀도·포인트 컬러는 2026-09-14 확정(§6.2.3). **계획 제시 → 컨펌 후** 이 절에 확정값을 적고 `globals.css`에 반영한다. 변경은 [`QA.md`](./QA.md)에 회차별로 기록한다.

---

## 7. Components

모두 라이브 CSS에서 실측한 값입니다. `[관측]`

### 7.1 Global Header

- 좌측: 국문 워드마크 + 패밀리사이트 스위처 `시대인재 ｜ N ｜ C ｜ BOOKS` (현재 사이트 외에는 회색 처리, `N`·`C`는 작은 사각 배지).
- 우측: 캠퍼스 드롭다운(`대치 ▾`), 주요 메뉴(`시간표`·`입시설명회`), 햄버거.
- `position: fixed`, 높이 **104px**, 좌우 패딩 100px, 최대폭 1512px, 배경 흰색, `z-index: 1000`.
- 로고 리스트 구분선 `1px × 16px`, 색 `#EFEFEF`. 로고 아이템 높이 20px.
- GNB 드롭다운: `min-width: 200px`, 패딩 `8px 0`, 항목 패딩 `12px 20px`, hover/active 배경 `#F5F5F5`.
- 로그인 링크 폭 76px.

### 7.2 Sub GNB `[관측]`

- 모바일에서 헤더 아래 고정되는 2차 내비(`시간표 · 입시설명회`). `position: fixed`, 높이 **50px**, 하단 보더 `1px #EFEFEF`, 리스트 폭 484px.

### 7.3 Hamburger Menu (전체 메뉴)

- 데스크톱: 우측 드로어 패널. 모바일: 전면 모달(`.modal-popup` 높이 100vh, radius 0).
- 헤더 패딩 `15px 40px`(sticky), 닫기 아이콘 32px. 콘텐츠 좌우 패딩 40px.
- 메뉴 그룹은 `1px #EFEFEF` 라인으로 구분: ① 시간표·입시설명회 ② 공지사항·부엉이포스트·문자수신 DB 등록 ③ **with SDIJ**(채용 · 출제·집필진 · 저작권 보호 센터 · 비즈니스 제안).
- 서브 항목 불릿은 `3×3px` 사각형 `#9F9F9F`, 활성 표시는 좌측 `4×24px` 바 `#000000`.
- 모션: 패널 `transform: translateX(100%) → 0`, `.4s ease-in-out`. 1280px 이상 폭 460px, 그 아래 100%. 딤 `rgba(0,0,0,.5)`은 페이드 없이 바로 깔린다.

### 7.4 Button

| 클래스 | 높이 | 패딩 | radius | 폰트 | 최소/최대폭 |
| --- | --- | --- | --- | --- | --- |
| `.btn` | **42px** | `8px 20px` | 2px | 16px/400, line-height 26px | min-width 100px |
| `.btn-large` | **58px** | `16px 20px` | 3px | 16px/400 | max-width 460px, width 100% |
| `.btn-responsive` | **38px** | `8px 20px` | 2px | 16px/400 | min-width 100px |
| `.alert-outlined-btn` | — | `16px 0` | — | 16px/400 | width 100% |

| 변형 | 기본 | hover | disabled |
| --- | --- | --- | --- |
| `filled` | 배경 `#000` / 글자 `#FFF` | 배경 동일(`#000`) | 배경 `#D4D4D4` / 글자 `#FFF` |
| `outlined` | 투명 / 글자 `#000` / 보더 `1px #D4D4D4` | 배경 `rgba(0,0,0,.05)` | 글자 `#D4D4D4` / 투명 |

- 공통: `display:inline-flex`, `gap: 2px`, `transition: all .2s ease`, `white-space: nowrap`.
- **태블릿·모바일(≤1279px)에서는 모든 버튼이 `width: 100%`** 로 늘어납니다.
- `[적용·시간표]` **시대인재 시간표** — 라벨이 있는 모든 버튼을 `<button>`·`<a>` 구분 없이 이 규격 하나로 그린다: `.btn` 42px, `.btn.sm` = `.btn-responsive` 38px, 16px/400, radius 2px, filled/outlined 두 변형. 폭 100%는 레이아웃이 깨지지 않는 폰(≤767px)의 모달·빈 상태 버튼에만 건다(태블릿은 데스크톱 배치 유지). filled hover는 누를 수 있다는 신호로 `#343D47`(Blue Black 80)을 남겼다. GNB 로그인은 버튼 박스가 아니라 메뉴와 같은 16px 글자 버튼(테두리 없음).
- `[적용·시간표]` **도구 버튼 `.btn.tool`**(2026-09-13) = 시간표 페이지 `.btn-notification`(문자수신 DB등록) 실측: `padding 8px 20px`, `1px #D4D4D4`, radius 2, 16px/400, lh 1.6, **최소폭 없음(내용 폭)**. 화면 안 보조 동작(학교 일정·전체 삭제·검색/조건 추가·더 보기)에 쓴다. 흐름 이동(이전/다음)·모달 확인은 `.btn` 그대로.

### 7.5 Primary Tabs (균등 분할)

- 컨테이너 100%, 아이템 배경 `#F5F5F5`, 상하 보더 `1px #D4D4D4`, 링크 패딩 `20px 16px`, 텍스트 16px/400 `#000`.
- **활성 탭**: 배경 `#FFFFFF`, 하단 보더 제거(콘텐츠와 연결되어 보이게).
- 항목이 많으면 `.tab-overflow`로 가로 스크롤되며, 이때 하단 라인은 `::after`(1px `#D4D4D4`)로 그리고 활성 탭만 `#FFFFFF`로 덮습니다.
- 실사용 예: `고3 / 고2 / 고1 / 예비고1(현 중3) / 수학스쿨 / 수학전문 라이벌매쓰 / 특목센터`.
- `[적용·시간표]` **시간표 흐름 단계 탭 `.steps`**(2026-09-13) — 왼쪽 세로 단계 표시를 폐기하고 이 규격으로 콘텐츠 위에 `1 내 스케줄 / 2 조건 선택 / 3 추천 시간표`를 3분할로 둔다. 높이 66px(패딩 20 + 글자 26), 현재 단계 흰 배경·아래 선 제거, 지난 단계는 검정 글자 + 클릭 복귀, 아직 안 온 단계는 `#9F9F9F`. 단계 번호는 22px 원 배지.

### 7.6 Secondary Tabs (Pill Chip)

- 아이템 높이 **38px**, 패딩 `6px 12px`, **radius 100px**, 리스트 gap 8px, 컨테이너 gap 24px.
- 활성: 배경 `#000000` + 글자 `#FFFFFF`. 비활성: 배경 투명 + 글자 `#9F9F9F`.
- 좌측 타이틀 16px/400 `#0B0B0B`, 구분선 `1px × 8px` `#D4D4D4`.
- 실사용 예: `정규 FINAL 시간표 / 추석특강 시간표 / FINAL 논술(수능전) / 콘텐츠 구성`.
- `[적용·시간표]` 시대인재 시간표의 선택 칩: 38px·radius 100px·활성 검정은 그대로, 비활성 글자 `#9F9F9F`(2.65:1)는 쓰지 않고 `#000` 글자 + `1px #D4D4D4` 보더로 대비와 "누를 수 있음"을 함께 준다.

### 7.7 Info Panel

- 배경 `#F5F5F5`, 패딩 `24px 20px`, **radius 0**.
- 키-값 정렬(라벨 굵게 + 값), 또는 불릿 안내문 목록.
- 실사용 예: 시간표 페이지 전화/문자 문의 박스, 컨설팅 안내 박스.

### 7.8 Data Table / List

- 헤더 행: 텍스트 중앙 정렬, 하단 보더 `1px`.
- 행 구분선 `1px #D4D4D4` 계열, 행 높이 넉넉하게(공지 목록 ≈ 71px).
- 컬럼 예(공지사항): `NO. / 제목 / 등록일 / 조회수`. 고정 공지는 번호 대신 **`공지`** 텍스트 배지(회색).
- 메타 값(날짜·조회수)은 `#9F9F9F` 계열, 제목은 `#000`.

### 7.9 Pagination

- 컨테이너 gap 30px, 번호 gap 24px(태블릿 20px, 모바일 10px).
- 번호 16px/400 `#9F9F9F`, hover `#000`.
- **활성**: 16px/**600** `#000` + `::after` 밑줄 `height 2px` `#000000`.
- 화살표 버튼 24×24px 이미지.

### 7.10 Form Controls

- **Input** — 보더 없음(래퍼가 보더 담당), 16px/400 `#000`. placeholder `#D4D4D4`. disabled 시 래퍼 배경 `#F5F5F5`, 글자 `#9F9F9F`.
- **Password Input** — 패딩 16px, 보더 `1px #D4D4D4`, radius 3px.
- **Textarea(글자수 제한형)** — `min-height: 400px`, 패딩 `16px 20px`, 보더 `1px #D4D4D4`, radius 5px, 내부 gap 30/40px. 카운터 14px/600 — 현재값 `#767676`, 구분자·최대값 `#9F9F9F`.
- **Dropdown** — option 16px/400, 패딩 16px. 미선택/`option[disabled]:checked` 상태는 글자 `#D4D4D4`, disabled는 배경 `#F5F5F5` + 글자 `#9F9F9F`.
- **Radio / Checkbox** — 20×20px 고정(`min-width/min-height` 동일), 항목 gap 8px.
- `[적용·시간표]` **커스텀 드롭다운** (시대인재 시간표 `dropdown.js`) — 브라우저 기본 목록을 쓰지 않는다. 트리거는 위 Dropdown 규격(보더 `1px #D4D4D4`, radius 3px, 화살표 우측 16px, hover·focus 보더 `#000`, disabled `#F5F5F5`/`#9F9F9F`)을 밀집 폼에 맞춰 **48px**로, 펼친 패널은 §7.1 GNB 드롭다운 규격(흰 배경, 그림자 `0 4px 20px rgba(0,0,0,.1)`, 패딩 `8px 0`, 항목 `12px 20px` 16px, hover `#F5F5F5`, 선택 항목 600 + 체크, 최대 320px 스크롤, 아래 공간이 없으면 위로 연다). 항목 개수 배지는 28×20px·12px/700·pill `#F5F5F5`, 0개 항목은 비활성. 복수 선택은 20px 체크 박스이고 닫힐 때 한 번만 값을 확정한다.
- `[적용·시간표]` **검색창** — 48px, 보더 `1px #D4D4D4`, radius 3px, 좌측 돋보기 22px, 16px 텍스트. 모달을 여는 트리거도 같은 모양이다.

### 7.11 Modal

- Dim `rgba(0,0,0,0.6)`.
- 모바일 팝업: `width 100%`, `height 100vh`, radius 0, 헤더 패딩 `14px 16px`(gap 20px), 닫기 32px, 콘텐츠 패딩 `0 16px 120px`, 푸터 패딩 `20px 16px`.
- 데스크톱(≥1280) 팝업 `[관측]`: 폭 **540px**, 패딩 **40px**, radius 5px, 최대 높이 90vh, 헤더 = 제목 24px/400 + 닫기 32px 아이콘, 제목 아래 40px, 푸터 위 40px(`modal.css`·문자수신 DB 등록 모달).
- `[적용·시간표]` **모든 모달 한 규칙**(2026-09-13) — 위 데스크톱 규격을 시간표 사이트의 모든 모달(`.share-modal`)에 적용한다: 540px·패딩 40·제목 24/400·닫기 32px(우상단 40/40)·딤 `rgba(0,0,0,.6)`(블러 없음)·하단 버튼(`.modal-foot`)은 1개면 전폭, 2개면 [outlined · filled] 반반. 내용이 넓은 강의 검색·시간표 이미지·개인정보방침만 `.wide` = **720px**(폭 외 규칙 동일). 모달 안 native `<select>`는 쓰지 않고 §7.10 커스텀 드롭다운, 긴 목록(학교)은 검색 모달로 고른다.
- `[적용·시간표]` **확인 모달** — 되돌릴 수 없는 동작(삭제·전체 삭제) 앞에서 제목 + 한 줄 설명 + [취소 outlined · 확인 filled] 2단, 같은 540px 규칙. 브라우저 기본 `confirm()`은 쓰지 않는다. 취소·Esc·딤 클릭은 모두 취소.

### 7.12 Footer

- 배경 **`#000000`**, 최대폭 1512px, 패딩 `80px 100px`(태블릿 `40px 0 80px`, 모바일 `40px 16px 80px`).
- 좌측: 워드마크(높이 17px) + 패밀리사이트 목록(`시대인재 N`, `시대인재 C`, `시대인재 BOOKS`).
- 우측: 캠퍼스별 전화번호 표(고3 / 고2·고1 / 라이벌매쓰 / 수학스쿨 / 특목센터), 정책 링크 행, 사업자 정보.
- 내부 구분선 `1px × 11px` `#434343`, 좌우 여백 8px.
- 사업자 정보: ㈜하이컨시 · 대표자 박근수 · 사업자등록번호 488-88-00381 · 서울특별시 강남구 도곡로 460, 동선빌딩 2층(대치동) · `sdij@hiconsy.com`.

---

## 8. Patterns

### 8.1 Hero Section `[관측]`

홈(`/aca/`)은 **히어로 한 화면 + 푸터**가 전부입니다.

- 흰 배경, 좌측 정렬 대형 영문 헤드라인 2줄(44px 이상, ExtraLight 계열) — "The New Paradigm / Navigate To Perfection".
- 우측 하단에 국문 본문 블록을 **별도 컬럼으로 분리**해 배치(4개 문단, 각 1~2줄).
- 그 사이 영문 부엉이 인용문이 작은 크기로 들어갑니다.
- 이미지·버튼·스크롤 인디케이터 없음. **여백이 유일한 장치.**

### 8.2 Page Header `[관측]`

- 좌측에 페이지 제목(예: `대치 시대인재 시간표`, `공지사항`, `비즈니스 제안`), 우측 상단에 아웃라인 버튼(`문자수신 DB등록`).
- 제목 아래 설명 문단(선택) → Info Panel 또는 Primary Tabs로 연결.
- 캠퍼스명이 제목에 접두로 붙습니다(`대치 입시설명회`, `대치 컨설팅`).

### 8.3 Detail View `[관측]`

- 상단 `#F5F5F5` 블록에 제목 → 구분선 → `등록일 YYYY.MM.DD`.
- 본문은 대부분 **업로드 이미지**(시간표·안내문). 본문 영역 폭 1080px, 하단 여백 240px.

### 8.4 Empty State `[관측]`

- 영역 중앙에 2줄 안내를 회색(`#9F9F9F` 계열)으로 표시.
- 실례: "진행 중인 설명회가 없습니다. / 문자수신 DB등록 신청 시 설명회 소식을 받으실 수 있습니다." — **빈 상태에서도 다음 행동을 안내**하는 문장을 붙입니다.

### 8.5 Auth / Verification Form `[관측]`

부엉이포스트 진입 시 본인확인 폼.

- 중앙 정렬 폼, 이름 입력 → `010 ▾` 드롭다운 + 번호 2칸 → **풀폭 검정 버튼(`인증 번호 받기`)** → 인증번호 입력(우측에 `3:00` 카운트다운) + `확인` 버튼.
- 하단 주석은 `※` 기호로 시작하는 회색 1줄.

### 8.6 Card `[관측]`

- 비즈니스 제안 페이지: 배경 `#F5F5F5`, radius 0, 제목(굵게) + 설명 1줄. 2열 그리드.

### 8.7 Mobile `[관측]`

- 헤더 축약(워드마크 + 캠퍼스 + 햄버거) + **서브 GNB 고정 바**.
- 좌우 패딩 16px, 버튼 풀폭, Primary/Secondary 탭 모두 **가로 스크롤**.
- 인포박스는 라벨-값 2열 유지(값 우측 정렬).

---

## 9. Content & Voice

### 9.1 Brand Philosophy `[관측]`

홈 히어로 카피 전문:

> 시대인재는 학생들의 시간가치와 결과가치를 이해하여 / 수많은 시간 정직한 공부를 하더라도 일부 학생들만 성공하는 냉혹한 입시 현실을 거슬러 / 한차원 다른 강의와 컨텐츠의 창조적 재배열을 통한 **Shortcut**의 실현으로 / 운의 영역을 초극하여 학생 자신의 **Hidden Score**를 실현하는 **만점구조시스템**을 구현합니다.

핵심 키워드 — **The New Paradigm · Navigate to Perfection · 자기초극 · Shortcut · Hidden Score · 만점구조시스템 · Education Engine**.
가이드라인 용어(§2.2)와 사이트 용어가 **일치**하므로 두 어휘 체계를 섞어 써도 됩니다. 단, 슬로건 표기는 가이드 기준 **`Navigate to Perfection`**(소문자 to)이고 사이트 히어로는 `Navigate To Perfection`(대문자 To)로 **표기가 갈립니다** — 신규 제작물은 가이드 표기를 따르십시오.

### 9.2 Tone of Voice

> ⚠️ **가이드라인 3.4.1 Tone of Voice는 "개발 예정"입니다.** 아래는 라이브 카피에서 귀납한 `[제안]`이며 확정 규정이 아닙니다.

- **명료하게** — 군더더기 없이 핵심을 먼저. 사실과 데이터로 말합니다.
- **차분하게** — 과장·자극적 표현을 피합니다.
- **존중하며** — 학습자를 압박하지 않는 어조.

| 권장 | 지양 |
| --- | --- |
| "성적 분석 리포트를 확인하세요." | "지금 안 보면 손해!" |
| "수업은 3월 2일에 시작합니다." | "역대급 커리큘럼 대공개!!!" |

### 9.3 Information Architecture `[관측]`

`sdij.com/aca` 실측 구조:

- **헤더** — 캠퍼스 선택(대치·목동·반포·분당·대전) · 시간표 · 입시설명회
- **햄버거** — 시간표 · 입시설명회 ／ 공지사항 · 부엉이포스트 · 문자수신 DB 등록 ／ **with SDIJ**: 채용 · 출제·집필진 · 저작권 보호 센터 · 비즈니스 제안
- **푸터** — 패밀리사이트(시대인재 N · C · BOOKS) · 캠퍼스 전화번호 · 채용 · 출제·집필진 · 저작권 보호 센터 · 비즈니스 제안 · 이용약관 · 개인정보처리방침
- 외부 연결: 채용 `/recruit/`, 출제·집필진 `/recruitment/`, 문항 공모 `/recruitment/content/`, 저작권 `/copyright/board.asp`, 저자 모집(BOOKS), 인증 `id.sdijon.com`

---

## 10. Accessibility

실측 색 조합의 명도 대비(WCAG 2.1):

| 조합 | 비율 | 판정 |
| --- | --- | --- |
| `#000000` on White | 21.00:1 | AAA ✅ |
| Blue Black `#010D19` on White | 19.57:1 | AAA ✅ |
| Navy `#052645` on White | 15.33:1 | AAA ✅ |
| Deep Gray `#293B47` on White | 11.60:1 | AAA ✅ |
| Purple `#38268A` on White | 11.59:1 | AAA ✅ |
| Teal `#0A5457` on White | 8.68:1 | AAA ✅ |
| `#606060` on White | 6.29:1 | AA ✅ |
| **`#767676` on White** | **4.54:1** | AA ⚠️ (기준 4.5:1를 0.04 차로 통과 — 더 밝게 만들지 말 것) |
| Cool Gray `#77878C` on White | 3.73:1 | 대형 텍스트만 ⚠️ |
| **`#9F9F9F` on White** | **2.65:1** | **FAIL ❌** |
| Sky Blue `#94BDD4` on White | 2.00:1 | FAIL ❌ (텍스트 금지) |
| Light Gray `#DBDFDE` on White | 1.34:1 | 비텍스트 전용 |
| White on Blue Black | 19.57:1 | AAA ✅ |
| Light Gray on Blue Black | 14.56:1 | AAA ✅ |
| Sky Blue on Blue Black | 9.78:1 | AAA ✅ |

**조치 사항**
- `#9F9F9F`는 현재 **비활성 칩·페이지네이션 번호·disabled 텍스트**에 쓰이고 있으며 본문 대비 기준에 미달합니다. 비활성 상태 표시는 색만으로 하지 말고 위치·굵기·밑줄 등 보조 단서를 함께 쓰십시오(페이지네이션 활성은 2px 밑줄을 함께 쓰므로 적절한 예).
- 마리골드 같은 유채 강조색이 없는 모노크롬 시스템이므로, **상태를 색만으로 구분하지 않는다**는 원칙이 특히 중요합니다.
- 터치 타깃 최소 44×44px — 현재 세컨더리 칩 높이 38px, 기본 버튼 42px로 **미달**합니다. 모바일에서는 세로 패딩 보강을 권합니다. `[제안]`
- Sub-Brand의 Sky Blue는 어두운 배경 위에서만 텍스트로 쓸 수 있습니다.

---

## 11. Assets & Resources

### 11.1 원본 자산

| 파일 | 내용 |
| --- | --- |
| `SDIJ_20250605_SDIJ_Guideline_Ver_1.pdf` | 브랜드 아이덴티티 가이드라인 v1.0 (55p) — 본 문서의 `[가이드]` 근거 |
| `SDIJ_20250605_SDIJ_Vector_Assets_Ver_1.ai` | 심볼·워드마크·시그니쳐 벡터 원본 (28MB) |
| `symbol_BLK.png` | 심볼 블랙 래스터 |

웹용 `SVG`, `PNG`(1x/2x/3x), 파비콘 세트를 `/assets/logo/`로 파생 관리하는 것을 권장합니다. `[제안]`

### 11.2 서체

- **Pretendard** — CDN(jsDelivr v1.3.9 static) 로드 중. Variable 사용 시 가중치를 §5.2.3 램프와 일치시킬 것.
- **Neue Haas Unica W1G** — 상용 서체. 인쇄·디자인 작업물 전용이며 **웹 라이선스 확보 여부 미확인**.

### 11.3 Token 운용

- 라이브 사이트에는 CSS 변수가 없습니다. 리팩터링 시 §5·§6 값을 `:root` 커스텀 프로퍼티로 먼저 추출하는 것을 권장합니다. `[제안]`
- 원시 컬러는 직접 쓰지 않고 §6 의미 토큰을 통해 참조합니다.

---

## 12. Changelog

| 버전 | 변경 내용 |
| --- | --- |
| 1.0 | 브랜드 가이드라인(.ai) 기반 초기 토큰·컴포넌트 정의 |
| 1.1 | 공식 웹사이트 기반 브랜드 철학·부엉이 모티브·IA 추가 |
| 2.0 | 라이브 사이트·관리자 콘솔 화면 분석 반영. Marketing/Console 표면 분리 |
| **3.3-AX.3** | **3회차 (2026-09-14).** 포인트 컬러 `#0074D2` 확정(코랄·`#0077C1` 삭제), wash 2단계 신설, 표 단일 규격(DataTable, 32px 행, 헤더 정렬), 검색창·커스텀 드롭다운 규격, 사이드바 접기·아바타, 페이지 끝 선 금지, 오류·404 셸 유지 (§6.2.3) |
| **3.3-AX.2** | **2회차 (2026-09-14).** radius 0/2/3/5 확정, 데이터 관리 5개 화면 D1 연결 |
| **3.3-AX** | **항해일지 AUTO 적용본 (2026-09-14).** 원본 v3.3에서 시간표 전용 컴포넌트(§7.13 격자·§7.14 결과 카드·햄버거/푸터/히어로 적용 불릿·`.btn-mini`)를 제거하고, 재사용 가능한 `[적용]` 규칙은 `[적용·시간표]`로 재태깅. §6.2를 AX 현재 코드 토큰 실측 + 브랜드 재매핑표 + UI 개편 착수 기록으로 교체 |
| 3.1 ~ 3.3 | 시대인재 시간표 MVP 적용 규칙(2026-09-13) — 버튼 단일 규격·도구 버튼(§7.4), 단계 탭(§7.5), 칩 대비(§7.6), 커스텀 드롭다운·검색창(§7.10), 모달 한 규칙·확인 모달(§7.11), 과목 칩. 상세는 원본 `sdij-design.md` |
| **3.0** | **가이드라인 원본 PDF(2025-06) + 라이브 사이트 CSS 실측으로 전면 교체.** 브랜드 플랫폼·심볼 의미·클리어스페이스(0.4x)·최소크기·사용 우선순위·로고 컬러 3순위·서브브랜드 규정·그래픽 모티프(Grid Line/Penrose) 신규. 컬러를 실제 16+16색 팔레트로 교체하고 존재하지 않던 `#0077C1`·`#231F20` 삭제. 타입 램프를 실측 19종으로 교체. 브레이크포인트 3단계·컨테이너 1512px·radius 2~3px·그림자 2종으로 정정. PDF 오탈자 3건 및 사이트-가이드 갭 명시 |

---

## 부록 A. 가이드라인이 아직 정의하지 않은 항목

가이드라인 v1.0에 **"개발 예정"으로 비워져 있는** 항목입니다. **이 영역은 임의로 규정을 만들지 말고, 필요 시 브랜드 담당 부서에 문의하십시오.**

| 항목 | 가이드 위치 |
| --- | --- |
| 전용 서체(Bespoke Typeface) | 2.3.1 |
| 레이아웃 시스템 | 3.2.1 |
| 브랜드 비주얼 및 환경 사진 가이드 | 3.3.1 |
| 인물 및 콘텐츠 사진 가이드 | 3.3.2 |
| 톤 오브 보이스 | 3.4.1 |

## 부록 B. 가이드 ↔ 라이브 사이트 갭

| # | 갭 | 상세 |
| --- | --- | --- |
| 1 | **브랜드 팔레트 미적용** | 사이트는 `#000000` 중심 순수 무채색. Blue Black·Cool Gray·Deep Gray 미사용(§5.1.3) |
| 2 | **영문 지정 서체 미적용** | Neue Haas Unica W1G 미로드. 영문도 Pretendard로 렌더(§5.2.3) |
| 3 | **그래픽 모티프 미적용** | Grid Line·Penrose 어느 것도 웹에 없음(§4) |
| 4 | **콘텐츠가 이미지** | 시간표·공지 본문이 외부 제작 이미지(보라/베이지·타 서체)라 디자인 시스템 밖. 텍스트 검색·반응형·접근성 모두 손실(§8.3) |
| 5 | **슬로건 표기 불일치** | 가이드 `Navigate to Perfection` ↔ 사이트 `Navigate To Perfection`(§9.1) |
| 6 | **CSS 토큰 부재** | `:root` 변수 없이 값 하드코딩(§11.3) |
| 7 | **접근성** | `#9F9F9F` 텍스트 2.65:1, 터치 타깃 38~42px(§10) |

---

*`[가이드]` 항목은 브랜드 아이덴티티 가이드라인 원본 규정으로 임의 변경할 수 없습니다.*
*`[관측]` 항목은 2026-09-06 라이브 사이트에서 실측한 값이며, `[제안]` 항목은 운영하며 보완하십시오.*
*`[적용·시간표]` 항목은 시대인재 시간표(`mvp-bwongdd`)에서 정한 규칙(2026-09-13)이고, `[적용·AX]` 항목은 항해일지 AUTO에서 정한 규칙이다. 사이트 원본과 다를 때는 이유를 함께 적는다.*
