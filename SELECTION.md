# SELECTION.md — 항해일지 수기 선별 기준 (정본)

> AI가 목차내용에 맞는 수기를 선별할 때 따르는 규칙의 단일 기준점.
> 관련 문서: 평가 기준 [`EVALUATION.md`](./EVALUATION.md) · 탈고 기준 [`REVISE.md`](./REVISE.md) · 진행 단계 [`PROCESS.md`](./PROCESS.md)
> 충돌 시 **선별에 관한 한 이 문서가 정본**이다.

---

## 1. 선별의 단위 — 작성자가 아니라 `qna` 행

**선별 단위는 `qna` 테이블의 각 행(질문 1개 + 답변 1개)이다.** 작성자(author)가 아니다.

| | 잘못된 모델 (기존) | 올바른 모델 (본 문서) |
| --- | --- | --- |
| 선별 대상 | 작성자 1명 | `qna` 행 1개 (answer_text) |
| 원고 본문 | 작성자의 전체 답변을 이어붙인 덩어리 | 선별된 답변 1개(또는 명시적으로 묶은 소수의 답변) |
| 중복 판정 키 | `author_id` | `qna.id` + 내용 지문 |

이유: 한 학생의 답변 중 "상반기 공부법"에 맞는 답변과 "수능 당일 멘탈"에 맞는 답변은 **서로 다른 목차에 실려야 한다.** 작성자 단위로 묶으면 이 분배가 불가능하다.

### 1.1 작성자 중복은 허용, 내용 중복은 금지

| 상황 | 허용 여부 |
| --- | --- |
| 같은 작성자의 **서로 다른 답변**이 다른 목차·다른 호차에 각각 실림 | ✅ **허용** |
| 같은 작성자의 답변 2개가 **같은 목차**에 실림 | ⚠️ 허용하되 경고 (편집자 판단) |
| **동일한 `qna` 행**이 두 곳에 실림 | ❌ **금지 (시스템 차단)** |
| 서로 다른 `qna` 행이지만 **실질적으로 같은 내용**이 다시 실림 | ❌ **금지 (본 문서 §2)** |

> 기존 `ax_manuscript.UNIQUE(author_id)` 제약은 위 원칙과 정면으로 충돌하므로 **제거한다.** 대신 `UNIQUE(qna_id)`로 대체한다.

---

## 2. 중복 게재 방지 — 3층 방어 ★

호차·파트·목차가 달라도 **같은 내용이 항해일지에 두 번 실려서는 안 된다.** 판정 범위는 **프로젝트(연도) 전체**이며, 호차 경계를 넘어 적용한다.

### L1. 하드 제약 — 동일 행 재게재 차단 (DB)

```sql
-- ax_manuscript
qna_id TEXT NOT NULL UNIQUE
```

- 이미 어느 목차에든 배치된 `qna_id`는 후보 목록 자체에서 제외한다.
- DB 제약이므로 UI 버그·동시 편집으로도 뚫리지 않는다.
- **후보 조회 쿼리에 반드시 포함**한다.
  ```sql
  WHERE n.id NOT IN (SELECT qna_id FROM ax_manuscript)
  ```

### L2. 근사 중복 — 내용 지문 대조 (결정적 계산)

동일 학생이 여러 질문에 같은 일화를 반복 서술하는 경우가 흔하다. 행이 달라도 내용이 같으면 걸러야 한다.

**정규화 규칙** (`content_hash` 생성용)

1. 공백·줄바꿈 전부 제거
2. 문장부호(`.,!?~…"'()[]{}·-` 등) 제거
3. 숫자는 유지 (점수·시기가 식별력이 높다)
4. 남은 문자열의 SHA-256 앞 16자 → `ax_manuscript.content_hash`

**판정**

| 조건 | 처리 |
| --- | --- |
| `content_hash` 완전 일치 | **자동 제외** (동일 내용) |
| 문자 5-gram Jaccard 유사도 ≥ 0.60 | **자동 제외** |
| 유사도 0.40 ~ 0.59 | 후보에 남기되 `duplicate_warning: true` — 편집자에게 경고 배지 노출 |
| 유사도 < 0.40 | 통과 |

### L3. 의미 중복 — LLM 판정 (에피소드 지문)

표현이 달라도 **같은 사건·같은 방법론**을 말하는 것은 중복이다. L2의 문자열 유사도로는 잡히지 않는다.

**에피소드 지문 4요소**를 추출해 비교한다.

| 요소 | 예 |
| --- | --- |
| `과목/영역` | 수학 미적분, 국어 비문학, 생활 루틴 |
| `시기` | 6월 모의고사 전후, 수능 D-30, 재수 시작기 |
| `사건` | 성적 급락, 선택과목 변경, 슬럼프, 수능 당일 실수 |
| `방법론` | 오답노트 3단계 회독, 서바이벌 활용법, 취침 시간 고정 |

**판정 규칙: 4요소 중 3개 이상이 기존 게재분과 일치하면 `is_duplicate: true` → 제외한다.**

LLM 호출 시 프롬프트에 **이미 확정된 게재분의 에피소드 지문 목록**을 반드시 주입한다(전체 프로젝트 범위, 호차 무관). 원문 전체가 아니라 지문 목록만 넣어 컨텍스트를 절약한다.

```
[이미 항해일지에 실린 내용 — 아래와 중복되는 수기는 절대 선별하지 말 것]
1. 1호차 Part1 Ch2 / 수학 미적분 · 6평 직후 · 성적 급락 · 오답노트 3단계 회독
2. 1호차 Part2 Ch1 / 국어 비문학 · 여름방학 · 시간 부족 · 지문 구조도 그리기
...
```

### L2·L3 적용 순서

```
후보 풀
  → L1: 이미 게재된 qna_id 제외 (SQL)
  → L2: content_hash · 유사도 계산으로 근사 중복 제외 (코드)
  → L3: LLM이 에피소드 지문 대조로 의미 중복 제외 (AI)
  → 목차 적합성 평가 (§3)
```

L1·L2를 먼저 돌려 LLM에 넘길 후보 수를 줄인다. 순서를 바꾸면 토큰만 낭비된다.

---

## 3. 목차 적합성 평가

중복을 통과한 후보에 대해서만 수행한다.

### 3.1 평가 항목

| 항목 | 가중 | 설명 |
| --- | --- | --- |
| **목차내용 적합성** | **최우선** | 편집자가 쓴 `목차내용`이 요구하는 주제·질문에 답이 되는가. 이 항목이 낮으면 다른 항목이 아무리 높아도 선별하지 않는다. |
| 정성과 구체성 | 높음 | 구체적 과목·시기·수치·본인만의 절차가 있는가 (EVALUATION.md 기준 준용) |
| 실용성 | 중간 | 다음 기수 재원생이 즉시 적용 가능한가 |
| 독창성 | 중간 | 흔한 클리셰를 벗어난 서사인가 |
| 가독성 | 낮음 | 탈고로 보완 가능하므로 배제 사유로 쓰지 않는다 |

작성자의 기존 `evaluations.total_score`는 **참고값**이다. 그 점수는 수기 전체에 대한 평가이고, 여기서 판단하는 것은 **개별 답변 하나가 이 목차에 맞는가**이므로 서로 다른 질문이다. 총점이 높다는 이유만으로 선별하지 않는다.

### 3.2 확정 신뢰도 (`confidence`)

편집자에게 "이건 그냥 써도 된다 / 검토가 필요하다"를 색으로 알린다.

| 등급 | 조건 | UI 색상 (DESIGN.md) |
| --- | --- | --- |
| `high` | 목차 적합성 5 + 구체성 충분. 탈고만 거치면 바로 게재 가능 | `status/success` `#1F9D3D` |
| `medium` | 적합하나 부분 발췌·보완 필요 | `status/info` `#0077C1` |
| `low` | 관련은 있으나 근거가 약함. 편집자 판단 필요 | `neutral` 회색 |

`duplicate_warning: true`인 후보는 신뢰도와 무관하게 `status/warning` 배지를 함께 단다.

### 3.3 정렬과 개수

- 편집자가 요청한 **선별 개수만큼만** 노출한다. 초과분은 버리지 않고 `대기` 상태로 보관해, 편집자가 후보를 제외했을 때 다음 순위를 채운다.
- 정렬 순서: `confidence` 내림차순 → `목차 적합성` 내림차순 → `구체성` 내림차순.
- **"사용하기 적합한 순서"가 곧 노출 순서**다. 작성자명·기수·점수 등으로 정렬하지 않는다.

---

## 4. 출력 스키마

LLM은 후보 1건마다 아래 JSON을 반환한다.

```json
{
  "qna_id": "qna_3f9a2b1c8d7e6f50",
  "author_id": "author_9c1d2e3f4a5b6c70",
  "is_duplicate": false,
  "duplicate_of": null,
  "duplicate_warning": false,
  "episode": {
    "subject": "수학 미적분",
    "period": "6월 모의고사 직후",
    "event": "성적 급락",
    "method": "오답노트 3단계 회독"
  },
  "fit_score": 5,
  "confidence": "high",
  "reason": "목차내용이 요구하는 '6평 전후 피드백 방법'에 정확히 대응하며, 오답 분류 기준을 3단계로 구체화해 다음 기수가 그대로 따라 할 수 있습니다.",
  "rank": 1
}
```

| 필드 | 설명 |
| --- | --- |
| `is_duplicate` | true면 후보에서 제외. `duplicate_of`에 충돌한 게재분 식별자를 남긴다 |
| `duplicate_warning` | L2 유사도 0.40~0.59 구간. 노출은 하되 경고 |
| `episode` | 4요소 지문. **DB에 저장해 다음 선별의 L3 입력으로 재사용한다** |
| `fit_score` | 목차 적합성 0~5 |
| `reason` | 편집자에게 보여줄 선별 이유. 1~2문장, 긍정적 활용 가능성 중심 (EVALUATION.md 톤 준용) |
| `rank` | 사용 적합 순위. 1이 가장 적합 |

`reason`은 **새로 생성되는 정보**다. `evaluations.evaluation_summary`(수기 전체 평가)를 재사용하지 않는다.

---

## 5. 후보 화면에 표시할 정보

후보는 작성자 카드가 아니라 **답변 카드**다. 아래 3종을 함께 보여준다.

1. **질문과 답변** — `questions.question_text` + `qna.answer_text` 전문
2. **작성자 정보** — `author_id` 기준 이름·유형(성적우수/성적향상/우선선발/포레스트)·최종 대학·평가 점수
3. **AI 선별 이유** — §4의 `reason` + 신뢰도 배지

---

## 6. 구현 체크리스트

- [ ] `ax_manuscript.qna_id` UNIQUE — L1 하드 제약
- [ ] `ax_manuscript.content_hash` — L2 정규화 해시
- [ ] `ax_manuscript.episode_json` — L3 에피소드 지문 보관
- [ ] `ax_manuscript.UNIQUE(author_id)` **제거** (작성자 중복 허용)
- [ ] 후보 쿼리에 `NOT IN (SELECT qna_id FROM ax_manuscript)` 포함
- [ ] LLM 프롬프트에 기존 게재분 에피소드 지문 주입 (프로젝트 전체 범위)
- [ ] 편집자가 선별을 취소하면 해당 `qna_id`가 즉시 후보 풀로 복귀

마지막 항목을 빠뜨리면 편집자가 뺀 수기가 영구히 사용 불가 상태로 남는다.
